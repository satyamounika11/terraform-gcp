# Toggle VM creation
enable_ec2_instance = true # set to false to create 0 instances

# Number of instances to create when enabled
instance_count = 3

# GCP project + location (same as before)
project_id = "sandbox-443817"
region     = "asia-south1"
zone       = "asia-south1-a"

